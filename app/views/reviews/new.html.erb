<%= form_for [@restaurant, @review] do |f| %>

    <%= f.label "Rating 1 - 5" %>:
    <%= f.number_field :rating, min: "0", max: "5"  %><br>

    <%= f.text_area :body, :rows => 10, :cols => 85, placeholder: "Add your review here" %>

    <%= f.submit %>
  <% end %>
